server:
  port: 8082

spring:
  application:
    name: SERVICE-Post

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/Postdb?createDatabaseIfNotExist=true
    username: "root"
    password: "root"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
