server:
  port: 8083

spring:
  application:
    name: SERVICE-FAVORITE

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
      jdbc:mysql://localhost:3306/favoritblog?createDatabaseIfNotExist=true
    username: "root"
    password: "root"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
